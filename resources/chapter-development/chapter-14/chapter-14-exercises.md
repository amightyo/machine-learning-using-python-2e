# Chapter 14 Extended Exercise Bank

- Manually tokenize and normalize several ambiguous sentences.
- Construct a document-term matrix by hand.
- Calculate TF, IDF, and TF-IDF for a small corpus.
- Calculate cosine similarity between two document vectors.
- Compare unigram and unigram+bigrams.
- Compare count vectors with TF-IDF.
- Inspect logistic-regression coefficients in a text classifier.
- Create a shortcut feature and observe its predictive effect.
- Compare person-level and document-level train/test splits.
- Explain static versus contextual embeddings using polysemous words.
- Compute a small attention matrix manually.
- Audit an AI claim that attention weights explain model reasoning.
- Design an education or health NLP study with a defensible generalization boundary.
