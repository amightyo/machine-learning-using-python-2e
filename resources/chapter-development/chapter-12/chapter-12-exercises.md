# Chapter 12 Extended Exercise Bank

- Compute a covariance matrix manually from a small dataset.
- Calculate eigenvalues/eigenvectors for a 2x2 covariance matrix and verify the principal direction.
- Compare PCA before and after standardization.
- Construct scree and cumulative explained-variance plots.
- Interpret loading patterns without over-naming components.
- Measure reconstruction error from 1 through p components.
- Compare logistic regression with and without PCA using a leakage-safe pipeline.
- Create t-SNE embeddings at several perplexity values and compare apparent structure.
- Repeat t-SNE under several seeds.
- Optionally compare PCA, t-SNE, and UMAP on the same dataset.
- Create a dataset with known continuous structure and audit whether a nonlinear embedding creates apparent islands.
- Ask AI to infer scientific categories from an embedding and rewrite its claims defensibly.
